/*
 * Multiply by Length - Edabit
 *
 * Create a function to multiply all values in an array
 * by the amount of values in that array.
 *
 * https://edabit.com/challenge/LMhRTq3hccz2D9Lax
 * github.com/fnogcps
 */

MultiplyByLength = a => a.map(b => a.length * b);
module.exports = {MultiplyByLength};
