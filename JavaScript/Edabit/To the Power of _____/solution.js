/* 
  * To the Power of _____ - Edabit
  *
  * Create a function that takes a base number and an exponent number 
  * and returns the calculation.
  *
  * https://edabit.com/challenge/H25aG5aAdmFcMpBsg
  * github.com/fnogcps
*/

calculateExponent = (a, b) => Math.pow(a, b)
module.exports = { calculateExponent }
