/*
 * Algebra Sequence — Boxes - Edabit
 *
 * Create a function that takes a number (step) as an argument and
 * returns the amount of boxes in that step of the sequence.
 *
 * https://edabit.com/challenge/Q4mYkd8nc8zdFBDE8
 * github.com/fnogcps
 */

boxSeq = a => (a % 2 ? a + 2 : a);
