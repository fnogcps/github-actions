/*
 * Maximum Edge of a Triangle - Edabit
 *
 * Create a function that finds the maximum range of a triangles third edge.
 *
 * https://edabit.com/challenge/nhXofMMyrowMyr9Nv
 * github.com/fnogcps
 */

nextEdge = (a, b) => a + b - 1;
module.exports = {nextEdge};
