/*
 * Repeat the Same Item Multiple Times - Edabit
 *
 * Create a function that takes two arguments (item, times).
 * The first argument (item) is the item that needs repeating
 * while the second argument (times) is the number of times the
 * item is to be repeated. Return the result in an array.
 *
 * https://edabit.com/challenge/BEHiM4RTmhcLX7Cq6
 * github.com/fnogcps
 */

repeat = (a, b) => Array(b).fill(a);
