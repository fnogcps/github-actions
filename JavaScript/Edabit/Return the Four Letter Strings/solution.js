/*
 * Return the Four Letter Strings - Edabit
 *
 * Create a function that takes an array of strings.
 * Return all words in the array that are exactly four letters.
 *
 * https://edabit.com/challenge/havnWL4BimvoaXNMn
 * github.com/fnogcps
 */

isFourLetters = a => a.filter(a => a.length == 4);
