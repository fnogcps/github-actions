/*
 * Check if String Ending Matches Second String - Edabit
 *
 * Create a function that takes two strings
 * and returns true if the first string ends with the second string;
 * otherewise return false.
 *
 * https://edabit.com/challenge/H9ohpGMyqJ9bAkqE5
 * github.com/fnogcps
 */

checkEnding = (a, b) => a.endsWith(b);
